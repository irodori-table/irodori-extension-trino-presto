name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Existing v* tag to rebuild and publish"
        required: true
        type: string

permissions:
  contents: write

jobs:
  extension-release:
    uses: irodori-table/irodori-kit/.github/workflows/extension-release.yml@v0.7.7
    with:
      release_tag: ${{ inputs.release_tag || github.ref_name }}
      duckdb_backed: false
    secrets:
      catalog_token: ${{ secrets.IRODORI_CATALOG_TOKEN }}
